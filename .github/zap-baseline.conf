# 10020: Missing Anti-clickjacking Header
10020	IGNORE	Static-site generator does not control deployment response headers.

# 10021: X-Content-Type-Options Header Missing
10021	IGNORE	Web-server header policy is owned by the eventual host or CDN.

# 10038: Content Security Policy Header Not Set
10038	IGNORE	CSP is enforced by deployment headers, not by the generated static files alone.

# 10049: Storable and Cacheable Content
10049	IGNORE	Caching policy is host-managed for this static site.

# 10063: Permissions Policy Header Not Set
10063	IGNORE	Permissions-Policy is configured at the host or header layer.

# 10109: Modern Web Application
10109	IGNORE	Framework fingerprinting is informational and not actionable for CI gating.

# 90004: Cross-Origin-Embedder-Policy Header Missing or Invalid
90004	IGNORE	COEP is a deployment header choice that the generated site cannot set itself.
